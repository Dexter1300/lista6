<Window x:Class="lista6.Window6"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:lista6"
        mc:Ignorable="d"
        Title="Window6" Height="450" Width="800">
    <Grid>
        <ListView Name="Views" Grid.Row="1">
            <ListView.View>
                <GridView AllowsColumnReorder="True">
                    <GridView.Columns>
                        <GridViewColumn Header="Id" 
                         DisplayMemberBinding="{Binding Id}"/>
                        <GridViewColumn Header="Imie"
                         DisplayMemberBinding="{Binding FirstName}"/>
                        <GridViewColumn Header="Nazwisko" 
                         DisplayMemberBinding="{Binding LastName}"/>
                        <GridViewColumn Header="Nr_klasy" 
                         DisplayMemberBinding="{Binding nr_klasy}"/>
                        <GridViewColumn Header="Kierunek" 
                         DisplayMemberBinding="{Binding kierunek}"/>
                    </GridView.Columns>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
